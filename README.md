# 🎥 OCTO-PI: Веб-система видеонаблюдения

> Веб-интерфейс для управления системой видеонаблюдения с распознаванием лиц и детекцией движения

---

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Учётные записи](#-учётные-записи)
- [Функционал](#-функционал)
- [Распознавание лиц](#-распознавание-лиц)
- [Режим записи](#-режим-записи)
- [Структура папок](#-структура-папок)
- [Устранение неполадок](#-устранение-неполадок)
- [Технические детали](#-технические-детали)

---

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Запуск
```bash
python launcher.py
```
Выберите `1` → Web version

### 3. Открыть в браузере
```
http://localhost:5000/login
```

---

## 👤 Учётные записи

| Роль | Логин | Пароль | Доступ |
|------|-------|--------|--------|
| **Администратор** | `admin` | `admin123` | Полный доступ |
| **Пользователь** | `user` | `user123` | Только просмотр |

---

## 🎛 Функционал

### 📊 Dashboard (Главная)
- Запуск/остановка системы видеонаблюдения
- Текущий статус системы

### 📺 Live (Трансляция)
- Просмотр всех камер в реальном времени
- Выбор конкретной камеры

### ⚙️ Settings (Настройки) — только Admin
| Опция | Описание |
|-------|----------|
| **Face Detection** | Распознавание лиц |
| **Motion Detection** | Детекция движения |
| **Recording** | Запись видео (всегда вкл.) |
| **Triggered Mode** | Запись только при движении |
| **Timeout** | Время ожидания между проверками |

### 📝 Logs (Логи) — только Admin
- Просмотр логов в реальном времени
- Фильтры: INFO, SUCCESS, WARNING, ERROR
- Фильтрация по дате

### 🧬 Biometric (Биометрия) — только Admin
- Обучение модели распознавания лиц
- Загрузка фотографий пользователей

---

## 🧠 Распознавание лиц

### Как это работает

| Ситуация | Цвет рамки | Текст |
|----------|------------|-------|
| ✅ Лицо распознано | 🟢 Зелёный | Имя человека |
| ❌ Лицо НЕ распознано | 🔴 Красный | "NE RASPOZNAN" |
| ⚠️ Модель не обучена | 🔴 Красный | "NE RASPOZNAN" |

### Обучение модели

**Шаг 1:** Создайте папки с фотографиями в `dataset/`:
```
dataset/
├── Иван_Иванов/
│   ├── foto1.jpg
│   ├── foto2.jpg
│   └── foto3.jpg
└── Мария_Петрова/
    ├── foto1.jpg
    └── foto2.jpg
```

**Шаг 2:** Обучите модель:
- Через веб: **Biometric** → **Обучить модель**
- Или в терминале:
```bash
python -c "from AI_face import learning; learning()"
```

**Результат:** Создадутся файлы `face_model.yml` и `labels.npy`

---

## 🎬 Режим записи

```
┌─────────────────────────────────────────────────────────┐
│  ПРЕЗАПИСЬ      │  ДВИЖЕНИЕ        │  ДОЗАПИСЬ         │
│  (4 сек)        │  (всё время)     │  (5 сек)          │
├─────────────────┼──────────────────┼───────────────────┤
│ ◀───────────────│──────────────────│─────────────▶     │
│ Сохраняется     │ Запись активна   │ После движения    │
│ в буфер         │                  │ ещё 5 секунд      │
└─────────────────────────────────────────────────────────┘
```

**Путь сохранения:** `recordings/ГГГГ-ММ-ДД/motion_detected/camX/`

---

## 📁 Структура папок

```
OCTO-PI/
├── 📄 launcher.py          # Точка входа
├── 📄 octo_web.py          # Веб-сервер
├── 📄 octo_cli.py          # Терминальная версия
├── 📄 config.py            # Конфигурация (индексы камер, ОС)
├── 📄 requirements.txt     # Зависимости
│
├── 📁 static/              # CSS, JS файлы
├── 📁 templates/           # HTML шаблоны
├── 📁 dataset/             # Фото для обучения
├── 📁 masks/               # Маски камер
├── 📁 recordings/          # Записи видео
└── 📁 logs/                # Логи системы
```

---

## 🔧 Устранение неполадок

### ❌ Ошибка: `pkgutil has no attribute 'ImpImporter'`

**Причина:** numpy несовместим с Python 3.12

**Решение:**
```bash
pip install "numpy>=1.26.0" --upgrade
```

---

### ❌ Ошибка: `unexpected keyword argument 'partitioned'`

**Причина:** Старая версия Werkzeug

**Решение:**
```bash
pip install "Werkzeug>=3.1.0" --upgrade
```

---

### ❌ Кнопки не работают (нет ошибок в консоли)

**Причина:** JavaScript загружался до DOM

**Решение:** Уже исправлено в текущей версии. Очистите кэш браузера: `Ctrl+Shift+R`

---

### ❌ Сервер запускается и сразу закрывается

**Причина:** flask-socketio не работает на Windows/Python 3.12

**Решение:** Уже исправлено — используется `app.run()` вместо `socketio.run()`

---

### ❌ Ошибка: "Порт 5000 занят"

**Симптом:** `Сделана попытка доступа к сокету методом, запрещенным правами доступа`

**Решение:**
```powershell
# Найти процесс
netstat -ano | findstr :5000

# Завершить (замените 12345 на PID)
taskkill /PID 12345 /F

# Перезапустить
python launcher.py
```

---

### ❌ Ошибка: `Error loading LBPH model`

**Причина:** Модель распознавания лиц не обучена

**Решение:** Это нормально! Система работает без распознавания. Чтобы включить:
1. Добавьте фото в `dataset/`
2. Обучите модель (см. раздел "Распознавание лиц")

---

## 🖥️ Кросс-платформенность

Система автоматически определяет ОС и настраивает индексы камер:

| ОС | Индексы камер | Устройства |
|----|---------------|------------|
| **Windows** | 0, 1, 2, 3 | Стандартные |
| **Linux (Ubuntu)** | 0, 2, 4, 6 | /dev/video0, /dev/video2... |
| **macOS** | 0, 1, 2, 3 | Стандартные |

Конфигурация находится в `config.py`. Для изменения индексов вручную:

```python
# config.py
CAMERA_INDICES = [0, 2, 4, 6]  # Ваши индексы
```

---

## ⚙️ Технические детали

| Компонент | Технология |
|-----------|------------|
| Backend | Flask 3.0 |
| Frontend | HTML5, CSS3, JavaScript |
| Видеопоток | MJPEG over HTTP |
| Распознавание лиц | OpenCV LBPH |
| Детекция движения | OpenCV |
| Логирование | Loguru |
| Кросс-платформенность | Автоопределение ОС |

### requirements.txt
```
Flask==3.0.0
flask-socketio==5.3.6
opencv-python>=4.8.1.78
numpy>=1.26.0
loguru==0.7.2
Werkzeug>=3.1.0
```

---

## 📌 Важные замечания

> ⚠️ **Маски:** Создание масок доступно только в терминальной версии (`python launcher.py` → режим 2). В веб-версии можно только просматривать существующие маски.

> ⚠️ **Камеры:** Система ожидает камеры с индексами 0, 1, 2, 3. Измените в коде при необходимости.

> ⚠️ **Windows:** На Windows используется Flask-сервер вместо SocketIO для стабильности.

---

<div align="center">

**OCTO-PI** | Система видеонаблюдения | 2025

</div>
